import styled from 'styled-components';

// COMPONENTES
export const SectionTitle = styled.h2`
  font-size: clamp(4.8rem, 10vw, 5.2rem);
  font-weight: 700;
`;

export const SectionText = styled.p`
  font-size: clamp(2rem, 3vw, 2.4rem);
  line-height: 1.5;
`;
